---
interface Props {
    title: string,
    description: string,
    image: string
};
const {title, description, image} = Astro.props;
---


<a class="hover:scale-[105%] transition-all duration-300 ease-in relative border border-white/80 bg-cover bg-center h-[300px] cursor-pointer" href="#" 
style={{backgroundImage: `linear-gradient(to bottom, hsla(0, 0%, 100%, 0.05), url(${image}.png))`}}>
</a>
<h1>hola</h1>